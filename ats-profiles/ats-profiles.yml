server:
  forward-headers-strategy: framework
spring:
  main:
    allow-bean-definition-overriding: true
  data.rest.base-path: api/v1
  datasource:
    url: jdbc:postgresql://localhost:5432/ats-profiles-dev?ssl=false
    username: user
    password: password
  jpa:
    properties:
      hibernate:
        generate_statistics: true
        cache:
          use_second_level_cache: true
          region.factory_class: org.hibernate.cache.ehcache.EhCacheRegionFactory
          queries: true
          ehcache:
            missing_cache_strategy: create
    database-platform: org.hibernate.dialect.PostgreSQL9Dialect
    show-sql: true
    hibernate:
      ddl-auto: update
      naming-strategy: org.hibernate.cfg.ImprovedNamingStrategy
  mail:
    smtp:
      timeout: 60000
    host: smtp.mandrillapp.com
    port: 587
    username: IAMAI
    password: md-ZpxXER_k9MwK0S8_x55eNQ
    admin: gladiypavlo@gmail.com
    base-link: https://dev.iamai.se/user/
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
management:
  endpoints:
    web:
      exposure:
        include: '*'
springdoc.api-docs.path: /api/v1/profiles/api-doc
logging:
  level:
    com.agenatech: DEBUG
health:
  config:
    enabled: false

server.servlet.encoding.charset: UTF-8
server.servlet.encoding.force-response: true
web.base-url: https://dev.iamai.se/
api.base-url: https://api-dev.iamai.se/api/v1